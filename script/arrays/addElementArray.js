const games = ['Resident Evil', "Hollow Knight", "Silent Hill"];

games.push("Shadow Man"); //push inserta un elemento al final del arreglo

for (let i = 0; i < games.length; i++) {
    console.log(games[i]);
}