const games = ['Resident Evil', "Hollow Knight", "Silent Hill"];

console.log(Array.isArray(games)); //Preguntamos si games es un arreglo. Devuelve un booleano

console.log(games instanceof Array); //Es lo mismo pero preguntamos si games es una instancia de la clase Array